# 参与 TBC

1. **先登记，再改。** 说明现象、证据（注入块原文、请求截取、模型回复），以及建议改哪一层（协议 / 实现 / 卡片）。
2. **协议改动必须同时带上：** 规范正文（用 必须 / 应该 / 可以）、`docs/conformance-zh.md` 登记表、`schema/` 语法或 Schema、`fixtures/` 里覆盖新规则的合规与不合规样例、`CHANGELOG.md` 的 Unreleased 条目、兼容说明。
3. **跑检查：** `npm install && npm test` 必须全绿。
4. **隐私：** 样例只用编造数值；不得提交真实心率、设备序列号、令牌、聊天内容。
5. **兼容：** `0.x` 期间同一小版本只加不改；改名必须保留旧名作别名至少一个小版本。
6. **实现：** 声称支持某版本的实现，应该在自己的测试里用 `tools/block.mjs` 校验生成的块，并实现对应版本的必需接口（v0.3 起包括 `tbc.diagnostics()`）。
